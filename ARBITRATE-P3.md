# 任务：独立仲裁 quality-v2 P3 标注复标分歧（替用户裁决）

仓库：`/home/song/code/Smartlect`（分支 main）。你是一个**独立仲裁人**：没有参与过此前的标注、开发或评审，正因如此你的判断才有价值。请通读下列材料后，对七个裁决项逐一给出**最终决定**。你的裁决记录将被另一会话照单执行（改数据集 → 重冻结 → v13 全量官方 run），所以决定要可执行、理由要站得住。

## 背景（最小必要）

- 本项目用 quality-v2 体系评测一个电商导购/客服/广告系统。客服线公开指标 `Faithfulness` 的分母 = 开发集 `checkable_claims` 中 `scope=essential` 的命题（独立 judge 判终答是否明确表达且与资料一致）；`peripheral` 只进诊断列。**essential 的判级线画在哪，是本次仲裁的核心问题。**
- 一位标注者（rater1，原始标注）与一次独立盲标（rater2，乱序去标签后重判）在 84 条 claim 上：一致率 0.857，Cohen's κ=0.633。分歧 12 条，聚成两个方向相反的族。
- 另有三个**行为金标**决定项（转人工哲学类），以及两条 needle（禁句）低风险标记。
- 广告线的"状态缝"已查明为评测 runner 测量 bug 并已修复，**不在**你的仲裁范围。

## 必读文件（按序，都是只读）

1. `ARBITRATION-P3.md` —— 仲裁清单本体：12 条分歧逐条明细、三个行为金标决定项、needle 标记、影响预览。你的裁决项 D1–D7 与其一一对应。
2. `evals/quality-v2/metrics-contract.md` —— 指标操作定义（重点：Faithfulness 节、禁句节、转人工 Pass 节）。
3. `artifacts/quality-v2/p3-relabel/compare-result.json` —— 机器可读分歧清单与分母影响。
4. `artifacts/quality-v2/p3-relabel/rater2-blind.jsonl` —— 盲标输入（题面+语料，**无原标签**）。如你想独立核验某条判级，先只看这个文件里的对应题，再回看第 5 项对照原标。
5. `evals/quality-v2/support/dev.jsonl` —— 数据集本体（原标注）。
6. `IMPLEMENTATION_STATUS.md` —— 全部历史与纪律。很长，重点扫"长期约束"与 2026-09-13 的 P3/T-A 条目。
7. `docs/quality-eval-v2.md` —— 体系索引。

行为金标项（D3–D5）如需实测证据（可选）：`artifacts/quality-v2/official-v11-20260913`、`official-v12-20260913`、`official-holdout2-20260913` 下 `support/`、`shopping/` 的逐试验文件。

## 已锁定，不要重开

- quality-v2 是唯一评测体系；合同 v6/v6.1 公开表头（导购 Pass@1 + Precision@4/ceiling；客服 Recall@8 + Faithfulness；广告 Attribution_integrity）；不合成总分。
- 方案 A 已落地：服务请求类问句+答案让步 → 控制器开工单（用户已拍板）。
- holdout-1/2 已"首测即终测"烧毁：**任何 holdout 文件与 manifest 不得改动**，不得据其调系统。
- 不为单个 badcase 打补丁；判级原则必须是类级的、能写进合同的，不能逐题各说各话。

## 你要裁决的七项

- **D1（族 A，9 条：原 essential → 复标 peripheral）**：与题面同文档同主题、但不落入"用户问的那件事"的守则/程序细节，算不算 essential。选项：全部维持原标 / 全部改复标 / 逐条混合（逐条给出终判）。清单见 ARBITRATION-P3.md §1（A1–A9，附复标者置信度）。
- **D2（族 B，3 条：原 peripheral → 复标 essential）**：多轮题中间轮的直接答案（judge 面=全问句拼接+末轮答案）。选项同上。清单见 §2（B1–B3）。
- **D3（handoff 软问法金标）**：是否在 dev 增补一道"软问法"题（如"什么时候补货"）；若增补，期望行为是 (a) 解释不建单=pass，还是 (b) 建单=pass。背景：方案 A 已收编硬索取类；holdout-2 的 sup-h2-05 软问法下模型诚实解释、零泄漏、未建单，按其金标判 fail。
- **D4（过期/撤回类，sup-d-05/57）**：(a) 维持现状（insufficient 算过、建单即 fail）；(b) 新增 `allow_handoff` 标注（insufficient 或建单均可，需改评分器+合同测试）；(c) 改 expected_handoff=true（必转人工）。背景：v11/v12 该类题行为双峰翻转的根源。
- **D5（shop-d-56 注入压价，导购线）**：(a) 维持诚实空集金标（拒注入+空集收口，转人工=fail）；(b) 接受 handoff 为可过行为（需给导购线加豁免语义）。注意 v12 与 holdout-2 中该行为已收敛通过。
- **D6（needle 两条标记）**：sup-d-46@0「退款已完成」（诚实答案"若查询显示退款已完成"会误命中）与 sup-d-51@0（"请勿…"的"勿"不在否定提示词集 不/没/未/非/别/无）。选项：各选 保留 / 改 needle 措辞；是否把 勿/莫 加入 NEGATION_CUES（评分器改动，影响全部禁句判定）。
- **D7（确认项）**：复合命题"半说"判法（judge 判面，jc-26/27 同族）本轮不动、随人审清单观察——确认或提出异议。

## 裁决原则

- 金标的首要品质是对被测系统的**公平性、判别力、跨 run 稳定性**，不是让分数好看，也不是迁就当前系统的行为。
- D1/D2 的裁决要落成**一条可写进合同的判级原则**（例如"essential=…"的一句话表述），再由该原则推出逐条终判，而不是逐条各找理由。
- 行为金标（D3–D5）要与已落地的方案 A 哲学自洽：想清楚"什么情况下建单是对的"这条线怎么画。
- 每项给出预期影响（essential 分母增减、哪些题的行为期望改变、是否需要评分器/合同改动）。
- 证据不足可以说"需要补什么证据再判"，但尽量给出倾向。

## 输出格式（直接输出，勿改动任何文件）

```markdown
## 裁决总表
| 项 | 决定 | 一句话理由 |
|---|---|---|
| D1 | … | … |
（D1–D7 各一行）

## 判级原则（拟入合同的一句话）
…

## 逐项详情
### D1
- 决定：…
- 终判表：case#claim_id → essential|peripheral（9 条全列）
- 理由：…
- 影响：essential 65→…，涉及题 …

### D2 …（同结构）
### D3–D7 …（决定/理由/影响/所需改动）

## 需要执行者做的数据集与评分器改动清单（逐条可执行）
…

## 风险与保留意见
…
```

## 纪律

- **只读**：不得修改、移动、删除任何文件（尤其 `holdout*`、`dev.jsonl`、manifest、freeze 产物）。
- 不跑评测、不起栈、不装依赖；离线阅读足够。
- 不重开已锁定事项，不提议新指标或合成总分。
- 全程用简体中文。
